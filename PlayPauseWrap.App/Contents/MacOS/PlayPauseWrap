#!/bin/bash
result=$(ps -ef | grep quickplay.py | grep -i python)
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}"  )" && pwd  )"
if [ -z "$result" -o -f /tmp/goToPandora ] ; then
    $DIR/play_pause_any.applescript
else
    echo pause | nc -u -w1 127.0.0.1 19999
fi
